import React from "react";
import Trips from "./components/Trips";

export const metadata = {
  title: "About",
};

const page = () => {
  return (
    <div>
      <Trips></Trips>
    </div>
  );
};

export default page;
